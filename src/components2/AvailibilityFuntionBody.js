import { useContext } from "react";
import DataContext from "../component/DataContext";

const AvailibilityFuntionBody = () => {
  const [items, setItems] = useContext(DataContext);

  return <div></div>;
};

export default AvailibilityFuntionBody;
